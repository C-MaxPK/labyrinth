export const selectIdGameCells = (state) => state.idGameCells;
export const selectStartPosition = (state) => state.startPosition;
export const selectEndPosition = (state) => state.endPosition;
export const selectShowResult = (state) => state.showResult;
